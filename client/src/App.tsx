/**
 * The application now uses the Next.js App Router under /app. This legacy
 * export remains only to avoid breaking editor references during migration.
 */
export default function LegacyApp() {
  return null;
}
